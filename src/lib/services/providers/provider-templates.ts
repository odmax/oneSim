// Provider templates removed — all providers are configured manually.
// No built-in provider templates. Every provider is added from scratch by the admin.
// The system knows zero real providers by default.
